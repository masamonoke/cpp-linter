const auto *pOtherText = static_cast<const PdfShapeText *>(other); // 0
auto p_parser = dynamic_cast<CPDF_Parser *>(p_renamedPage->GetQPDFDocument()->CPDFDocument()->GetParser()); // 0
auto p_page = static_cast<QTabPage *>(widget(i)); // 0
auto *pPage = new DocPage(PDFPermissions, m_defaultLayer, nullptr, page, nullptr, pDocument); // 0

